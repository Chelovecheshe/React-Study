import styled from "styled-components";

const SnackbarSettingsWrapper = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-around;
  padding-top: 25px;
`;

export default SnackbarSettingsWrapper;
